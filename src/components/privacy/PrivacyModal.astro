---
import Layout from '../../layouts/Layout.astro';
---
<Layout 
  title="Políticas de privacidad - Josue Dev" 
  description="Conoce cómo protegemos tu información personal y tus derechos de privacidad."
  showMenu={false}
>

<div id="privacy-modal" class="privacy-modal hidden">
  <div class="modal-overlay">
    <div class="modal-container">
      <div class="modal-header">
        <h2 class="modal-title">Política de Privacidad</h2>
        <button id="close-modal" class="close-button">
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
          </svg>
        </button>
      </div>
      
      <div class="modal-content">
        <div class="privacy-summary">
          <h3>Resumen de Privacidad</h3>
          <p>Tu privacidad es mi prioridad. Esta política explica cómo manejo tu información personal de manera segura y transparente.</p>
        </div>

        <div class="privacy-sections">
          
          <div class="section">
            <h4>🔒 ¿Qué información recopilo?</h4>
            <ul>
              <li><strong>Información de contacto:</strong> Nombre, email, teléfono, empresa</li>
              <li><strong>Detalles del proyecto:</strong> Tipo, presupuesto, timeline, descripción</li>
              <li><strong>Datos técnicos:</strong> IP, navegador (solo para mejorar la experiencia)</li>
            </ul>
          </div>

          <div class="section">
            <h4>🎯 ¿Cómo la uso?</h4>
            <ul>
              <li>Para responder a tus consultas y evaluar proyectos</li>
              <li>Para crear propuestas personalizadas</li>
              <li>Para comunicación durante el desarrollo del proyecto</li>
              <li><strong>NO la vendo ni comparto con terceros</strong></li>
            </ul>
          </div>

          <div class="section">
            <h4>🛡️ ¿Cómo la protejo?</h4>
            <ul>
              <li>Cifrado HTTPS en todas las comunicaciones</li>
              <li>Almacenamiento seguro en servidores certificados</li>
              <li>Acceso limitado solo a información necesaria</li>
              <li>Eliminación automática según políticas de retención</li>
            </ul>
          </div>

          <div class="section">
            <h4>⚖️ Tus derechos</h4>
            <ul>
              <li><strong>Acceso:</strong> Ver qué información tengo sobre ti</li>
              <li><strong>Corrección:</strong> Actualizar datos incorrectos</li>
              <li><strong>Eliminación:</strong> Solicitar borrado de tus datos</li>
              <li><strong>Portabilidad:</strong> Recibir tus datos en formato estructurado</li>
            </ul>
          </div>

          <div class="section highlight">
            <h4>📧 Contacto para ejercer derechos</h4>
            <p>
              <strong>Email:</strong> <a href="mailto:esymbel.mkt2025@gmail.com">esymbel.mkt2025@gmail.com</a><br>
              <strong>Tiempo de respuesta:</strong> Máximo 30 días<br>
              <strong>Asunto:</strong> "Ejercicio de Derechos de Privacidad"
            </p>
          </div>

        </div>

        <div class="legal-notice">
          <p><strong>Base legal:</strong> Procesamiento basado en consentimiento informado y interés legítimo para gestión comercial.</p>
          <p><strong>Retención:</strong> Datos de proyectos se conservan 3 años, datos técnicos 12 meses.</p>
          <p><strong>Última actualización:</strong> 17 de octubre de 2025</p>
        </div>
      </div>

      <div class="modal-footer">
        <a href="/privacy" target="_blank" class="full-policy-link">
          Ver política completa
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"/>
          </svg>
        </a>
        <button id="accept-privacy" class="accept-button">
          Entendido
        </button>
      </div>
    </div>
  </div>
</div>
</Layout>

<style>
  /* 🎨 ESTILOS DEL MODAL DE PRIVACIDAD */
  
  .privacy-modal {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    z-index: 9999;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 1rem;
  }

  .privacy-modal.hidden {
    display: none;
  }

  .modal-overlay {
    position: absolute;
    inset: 0;
    background: rgba(0, 0, 0, 0.8);
    backdrop-filter: blur(8px);
    animation: fadeIn 0.3s ease;
  }

  .modal-container {
    background: linear-gradient(135deg, #1e293b 0%, #0f172a 100%);
    border: 1px solid rgba(59, 130, 246, 0.2);
    border-radius: 16px;
    max-width: 800px;
    max-height: 85vh;
    width: 100%;
    position: relative;
    z-index: 1;
    overflow: hidden;
    animation: slideUp 0.4s ease;
    box-shadow: 0 25px 50px rgba(0, 0, 0, 0.5);
    display: flex;
    flex-direction: column;
  }

  @keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
  }

  @keyframes slideUp {
    from { 
      opacity: 0;
      transform: translateY(30px) scale(0.95); 
    }
    to { 
      opacity: 1;
      transform: translateY(0) scale(1); 
    }
  }

  .modal-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 1.5rem 2rem;
    border-bottom: 1px solid rgba(59, 130, 246, 0.2);
    background: linear-gradient(135deg, rgba(59, 130, 246, 0.1), rgba(139, 92, 246, 0.1));
    flex-shrink: 0;
  }

  .modal-title {
    color: #f1f5f9;
    font-size: 1.5rem;
    font-weight: 700;
    margin: 0;
    background: linear-gradient(135deg, #60a5fa, #a78bfa);
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .close-button {
    background: none;
    border: none;
    color: #94a3b8;
    cursor: pointer;
    padding: 0.5rem;
    border-radius: 8px;
    transition: all 0.3s ease;
  }

  .close-button:hover {
    background: rgba(239, 68, 68, 0.1);
    color: #ef4444;
    transform: scale(1.1);
  }

  .modal-content {
    padding: 2rem;
    flex: 1;
    overflow-y: auto;
    scrollbar-width: thin;
    scrollbar-color: rgba(59, 130, 246, 0.3) transparent;
    min-height: 0;
  }

  .modal-content::-webkit-scrollbar {
    width: 6px;
  }

  .modal-content::-webkit-scrollbar-track {
    background: transparent;
  }

  .modal-content::-webkit-scrollbar-thumb {
    background: rgba(59, 130, 246, 0.3);
    border-radius: 3px;
  }

  .privacy-summary {
    background: rgba(34, 197, 94, 0.1);
    border: 1px solid rgba(34, 197, 94, 0.3);
    border-radius: 12px;
    padding: 1.5rem;
    margin-bottom: 2rem;
  }

  .privacy-summary h3 {
    color: #22c55e;
    margin-bottom: 0.75rem;
    font-size: 1.1rem;
    font-weight: 600;
  }

  .privacy-summary p {
    color: #cbd5e1;
    margin: 0;
    line-height: 1.6;
  }

  .privacy-sections {
    space-y: 1.5rem;
  }

  .section {
    background: rgba(30, 41, 59, 0.3);
    border: 1px solid rgba(59, 130, 246, 0.1);
    border-radius: 12px;
    padding: 1.5rem;
    margin-bottom: 1.5rem;
    backdrop-filter: blur(10px);
  }

  .section.highlight {
    background: rgba(139, 92, 246, 0.1);
    border-color: rgba(139, 92, 246, 0.3);
  }

  .section h4 {
    color: #60a5fa;
    margin-bottom: 1rem;
    font-size: 1rem;
    font-weight: 600;
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .section ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .section li {
    color: #e2e8f0;
    padding: 0.5rem 0;
    padding-left: 1.25rem;
    position: relative;
    line-height: 1.5;
  }

  .section li::before {
    content: "•";
    position: absolute;
    left: 0;
    color: #3b82f6;
    font-weight: bold;
  }

  .section p {
    color: #cbd5e1;
    line-height: 1.6;
    margin: 0;
  }

  .section a {
    color: #a78bfa;
    text-decoration: none;
    font-weight: 600;
  }

  .section a:hover {
    color: #c4b5fd;
    text-decoration: underline;
  }

  .legal-notice {
    background: rgba(15, 23, 42, 0.6);
    border: 1px solid rgba(71, 85, 105, 0.3);
    border-radius: 8px;
    padding: 1rem;
    margin-top: 1.5rem;
  }

  .legal-notice p {
    color: #94a3b8;
    font-size: 0.85rem;
    margin: 0.25rem 0;
    line-height: 1.5;
  }

  .modal-footer {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 1.5rem 2rem;
    border-top: 1px solid rgba(59, 130, 246, 0.2);
    background: rgba(15, 23, 42, 0.5);
    flex-shrink: 0;
  }

  .full-policy-link {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    color: #60a5fa;
    text-decoration: none;
    font-weight: 500;
    transition: all 0.3s ease;
    padding: 0.5rem 1rem;
    border-radius: 8px;
  }

  .full-policy-link:hover {
    background: rgba(59, 130, 246, 0.1);
    color: #93c5fd;
  }

  .accept-button {
    background: linear-gradient(135deg, #3b82f6 0%, #8b5cf6 100%);
    color: white;
    border: none;
    padding: 0.75rem 2rem;
    border-radius: 8px;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s ease;
    box-shadow: 0 4px 12px rgba(59, 130, 246, 0.3);
  }

  .accept-button:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 20px rgba(59, 130, 246, 0.4);
  }

  /* Responsive */
  @media (max-width: 768px) {
    .privacy-modal {
      padding: 0.5rem;
    }

    .modal-container {
      max-width: 100%;
      max-height: 90vh;
      margin: 0;
    }

    .modal-header,
    .modal-content,
    .modal-footer {
      padding: 1rem;
    }

    .modal-footer {
      flex-direction: column;
      gap: 1rem;
    }

    .full-policy-link,
    .accept-button {
      width: 100%;
      justify-content: center;
    }

    .section {
      padding: 1rem;
    }
  }
</style>

<script>
  // 🎯 FUNCIONALIDAD DEL MODAL DE PRIVACIDAD
  
  // Declaraciones de tipos para funciones globales
  declare global {
    interface Window {
      openPrivacyModal: () => void;
      closePrivacyModal: () => void;
    }
  }
  
  // Función para abrir el modal
  function openPrivacyModal() {
    const modal = document.getElementById('privacy-modal');
    if (modal) {
      modal.classList.remove('hidden');
      document.body.style.overflow = 'hidden';
      
      // Focus en el primer elemento
      const closeButton = modal.querySelector('#close-modal') as HTMLElement;
      if (closeButton) {
        closeButton.focus();
      }
    }
  }

  // Función para cerrar el modal
  function closePrivacyModal() {
    const modal = document.getElementById('privacy-modal');
    if (modal) {
      modal.classList.add('hidden');
      document.body.style.overflow = '';
    }
  }

  // Event listeners cuando el DOM esté listo
  document.addEventListener('DOMContentLoaded', function() {
    const modal = document.getElementById('privacy-modal');
    const closeButton = document.getElementById('close-modal');
    const acceptButton = document.getElementById('accept-privacy');
    const overlay = modal?.querySelector('.modal-overlay');

    // Cerrar con botón X
    if (closeButton) {
      closeButton.addEventListener('click', closePrivacyModal);
    }

    // Cerrar con botón Entendido
    if (acceptButton) {
      acceptButton.addEventListener('click', closePrivacyModal);
    }

    // Cerrar al hacer clic en el overlay
    if (overlay) {
      overlay.addEventListener('click', function(e) {
        if (e.target === overlay) {
          closePrivacyModal();
        }
      });
    }

    // Cerrar con tecla Escape
    document.addEventListener('keydown', function(e) {
      if (e.key === 'Escape' && modal && !modal.classList.contains('hidden')) {
        closePrivacyModal();
      }
    });

    // Hacer la función global para que pueda ser llamada desde otros scripts
    window.openPrivacyModal = openPrivacyModal;
    window.closePrivacyModal = closePrivacyModal;
  });
</script>