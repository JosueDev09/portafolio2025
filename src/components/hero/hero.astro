---
import './hero.css';
---


<header class="w-full h-screen hero">
     <div class="hero-content">
    <div class="hero-grid">
      <div class="hero-text">
        <h1>JOSUE ; DEV</h1>
        <p>Desarrollador Web Full Stack</p>
      </div>
      <div class="hero-image">
        <div class="image-decoration-1"></div>
        <div class="image-decoration-2"></div>
        <img src="/assets/imgConvertidas/hero2.webp" alt="Josue Profile" class="hero-profile-img">
        <div class="image-decoration-3"></div>
      </div>
    </div>
  </div>
</header>

<script>
  const hero = document.querySelector(".hero") as HTMLElement;
  const content = document.querySelector(".hero-content") as HTMLElement;

  // 🟢 Animación de entrada al cargar
  window.addEventListener("load", () => {
    setTimeout(() => {
      if (content) {
        content.classList.add("show");
      }
    }, 200); // pequeño delay para suavizar
  });

  // 🟡 Animación de salida al hacer scroll
  window.addEventListener("scroll", () => {
    const scrollY = window.scrollY;
    const heroHeight = hero.offsetHeight;

    const progress = Math.min(scrollY / heroHeight, 1);

    const translateY = progress * -100; // movimiento más natural
    const opacity = 1 - progress * 1.2;

    if (scrollY < heroHeight) {
      content.style.transform = `translateY(${translateY}px)`;
      content.style.opacity = opacity < 0 ? "0" : opacity.toString();
    }
  });
</script>